This module implements some code that was removed from subsites, specifically related pages and recording of master page when copying a subsite.

This exists incase anyone still needs that functionality.
